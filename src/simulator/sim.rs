pub struct Simulator {
    // input packet stream
// algorithm
}

impl Simulator {
    pub fn new() -> Simulator {
        Simulator {}
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn test_simple() {
        assert!(true);
    }
}
